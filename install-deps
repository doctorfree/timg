#!/bin/bash

sudo apt install cmake git g++ pkg-config -q -y
sudo apt install libgraphicsmagick++-dev libturbojpeg-dev libexif-dev \
                 libswscale-dev libdeflate-dev librsvg2-dev libcairo-dev  -q -y

# For sixel output.
sudo apt install libsixel-dev -q -y

# If you want to include video decoding, also install these additional libraries
# sudo apt install libavcodec-dev libavformat-dev -q -y

# sudo apt install libavdevice-dev -q -y # If you want to read from video devices such as v4l2

#sudo apt install libopenslide-dev -q -y # If you want to add OpenSlide images support
#sudo apt install libpoppler-glib-dev -q -y  # if WITH_POPPLER enabled.

sudo apt install pandoc -q -y  # If you want to recreate the man page
